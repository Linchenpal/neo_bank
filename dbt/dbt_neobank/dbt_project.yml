name: dbt_neobank
version: '1.0.0'

profile: dbt_neobank  # 👈 match my ~/.dbt/profiles.yml

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_neobank:  # 👈 this must match the project `name:` above
    staging:
      +schema: neobank_data_staging
      +materialized: view
    vault:
      +materialized: table
